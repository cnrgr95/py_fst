# Flask Configuration
FLASK_ENV=development
SECRET_KEY=your-secret-key-here-change-in-production-make-it-long-and-random
DEBUG=True

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=py_fst
DB_USER=postgres
DB_PASSWORD=your-database-password-here

# Application Configuration
APP_HOST=127.0.0.1
APP_PORT=5000

# Session Configuration
PERMANENT_SESSION_LIFETIME=3600

# Security Configuration (Production)
SESSION_COOKIE_SECURE=False
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SAMESITE=Lax
REMEMBER_COOKIE_SECURE=False
REMEMBER_COOKIE_HTTPONLY=True

# CSP Configuration
CSP_DEFAULT_SRC='self'
CSP_SCRIPT_SRC='self' cdn.jsdelivr.net cdnjs.cloudflare.com
CSP_STYLE_SRC='self' cdn.jsdelivr.net cdnjs.cloudflare.com 'unsafe-inline'
CSP_IMG_SRC='self' data:
CSP_FONT_SRC='self' cdnjs.cloudflare.com fonts.gstatic.com fonts.googleapis.com